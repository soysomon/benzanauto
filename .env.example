# URL absoluta opcional del backend.
# Si la dejas vacía, el frontend usará /api en el mismo origen.
VITE_API_URL=

# Solo para desarrollo local con Vite proxy.
VITE_DEV_PROXY_TARGET=http://localhost:4000
